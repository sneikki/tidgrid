{
  "name": "tidgrid",
  "version": "1.0.0",
  "main": "src/index.scss",
  "repository": "git@github.com:varvikko/tidgrid.git",
  "author": "Nuutti Varvikko <nvarvikko@gmail.com>",
  "license": "MIT",
  "scripts": {
    "build": "npm run compile",
    "compile": "sass src/index.scss build/tidgrid.css --load-path=src",
    "compile:minimized": "npm run compile -- --style=compressed",
    "test": "jest",
    "lint:scss": "stylelint \"{src,test}/**/*.scss\"",
    "fix:scss": "npm run lint:scss -- --fix",
    "clean": "rm -rf build/"
  },
  "devDependencies": {
    "glob": "^8.1.0",
    "jest": "^29.4.1",
    "postcss": "^8.4.21",
    "sass": "^1.57.1",
    "sass-true": "^7.0.0",
    "stylelint": "^14.16.1",
    "stylelint-config-standard-scss": "^6.1.0"
  }
}
